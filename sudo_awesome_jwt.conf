# Where the token lives
token_file = /run/jenkins-sudo/token

# Public key for verification (PEM)
public_key = /etc/sudo-jwt/jenkins.pub

# Required claims
issuer   = "jenkins.sippysoft.com"

# Literal audience
audience = "sudo@prod-host"

# The audience read from a file
# audience = /etc/sudo-jwt/audience

# Max token age (seconds)
max_ttl = 300

# Required scope
scope = "sudo"

# Optional hardening
require_tty = false

# require_jwt = true

# Literal command_allowlist (CSV)
# command_allowlist = "/bin/scripts/script1.sh,/bin/scripts/script2.sh"

# The command_allowlist read from a file
# command_allowlist = /etc/sudo-jwt/command_allowlist

# only_user = "jenkins"
# only_uid = 1001
# host = "prod-b2bua-01"
